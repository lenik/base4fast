# Seed install only — packages land in PNPM_STORE_DIR; node_modules is deleted after.
# Upper apps: pnpm install --prefer-offline (default) or --offline (strict).
# package-import-method left unset → pnpm auto (clone/hardlink/copy).
store-dir=../pnpm-store
registry=https://registry.npmmirror.com
