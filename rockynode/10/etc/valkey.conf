# Valkey (Redis-compatible) defaults for b4f-rockynode.
# Override: mount a file to /etc/rockynode.d/valkey.conf
bind 0.0.0.0
protected-mode no
port 6379
daemonize no
dir /var/lib/valkey
dbfilename dump.rdb
save 60 1000
appendonly no
loglevel notice
